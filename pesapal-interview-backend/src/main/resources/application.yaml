spring:
  application:
    name: pesapal-interview
  datasource:
    url: jdbc:postgresql://localhost:7703/pesapal
    username: postgres
    password: 12345678
    driver-class-name: org.postgresql.Driver
  jpa:
#    show-sql: true
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: dev
server:
  port: 8080
logging:
  level:
    org:
      hibernate:
        SQL: Debug
        orm:
          jdbc:
            bind: TRACE
---
spring:
  config:
    activate:
      on-profile: deployment
  datasource:
    url: jdbc:postgresql://pesapal_pgbouncer:6432/pesapal
    username: postgres
    password: 12345678
    driver-class-name: org.postgresql.Driver

